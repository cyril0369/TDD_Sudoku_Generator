version: "3.9"

services:
  api:
    build: .
    container_name: sudoku-api
    ports:
      - "8000:8000"

  front:
    image: nginx:alpine
    container_name: sudoku-front
    ports:
      - "3000:80"
    volumes:
      - ./front:/usr/share/nginx/html
    depends_on:
      - api